{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the app window and the menu-bar panel.",
  "windows": [
    "main",
    "panel"
  ],
  "permissions": [
    "core:default"
  ]
}
